# Hands-on Challenges

1. Scale a deployment to 5 replicas and verify pod distribution.
2. Add a new environment variable via ConfigMap and consume it in app response.
3. Add a new internal service and route traffic using Ingress path rules.
